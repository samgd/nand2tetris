/**
 * And4Way gate:
 * out = 1 if (a == 1 and b == 1 and c == 1 and d == 1)
 *       0 otherwise
 */

CHIP And4Way {
    IN a, b, c, d;
    OUT out;

    PARTS:
    And(a=a,      b=b, out=andAB);
    And(a=andAB,  b=c, out=andABC);
    And(a=andABC, b=d, out=out);
}
